// Mock abstractions that can be loaded into the RuntimeBuilder during testing
pub struct MockEventBus;
pub struct MockCommandBus;
pub struct MockStorageProvider;
pub struct MockCommunicationProvider;
