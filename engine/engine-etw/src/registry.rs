pub struct EtwProviderRegistry {
    // Maps Provider GUIDs to parsers and capabilities
}

pub struct EtwParserRegistry {
    // Decoupled registry explicitly tracking EventRecordParser implementations
}
